import styled from "styled-components";

const StyledFooter = styled.footer`
  display: flex;
  text-align: center;
  gap: 30px;
`

const Footer = () => {
  <StyledFooter>
    
  </StyledFooter>
}

export default Footer;